# Copyright 2026 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/components.gni")

import("//build/devicetree/devicetree_visitor.gni")

group("tests") {
  deps = [
    "test:example-visitor-test($host_toolchain)",
    "test:example-visitor-test-package",
  ]
  testonly = true
}

devicetree_visitor("example-visitor") {
  sources = [
    "example-visitor.cc",
    "example-visitor.h",
  ]
  deps = [
    "//sdk/lib/driver/devicetree/visitors:driver-visitor",
    "//sdk/lib/driver/devicetree/visitors:registration",
  ]
}
