// WARNING: This file is machine generated by fidlgen.

// fidl_experiment = output_index_json

#pragma once

#include <fidl/test.zxtypes/cpp/common_types.h>
#include <lib/fidl/cpp/features.h>

#if __FIDL_SUPPORT_FORMAT
#include <format>

template <>
struct std::formatter<::test_zxtypes::StatusEnum> {
  constexpr auto parse(auto& ctx) { return ctx.begin(); }
  auto format(const ::test_zxtypes::StatusEnum& value, std::format_context& ctx) const {
    std::string_view output;
    switch (value) {
      case ::test_zxtypes::StatusEnum::kOk:
        output = "kOk";
        break;
      default:
        output = "UNKNOWN";
    }
    return std::format_to(ctx.out(), "test_zxtypes::StatusEnum::{}", output);
  }
};
template <>
struct std::formatter<::test_zxtypes::StatusEnumAlias> {
  constexpr auto parse(auto& ctx) { return ctx.begin(); }
  auto format(const ::test_zxtypes::StatusEnumAlias& value, std::format_context& ctx) const {
    std::string_view output;
    switch (value) {
      case ::test_zxtypes::StatusEnumAlias::kOk:
        output = "kOk";
        break;
      default:
        output = "UNKNOWN";
    }
    return std::format_to(ctx.out(), "test_zxtypes::StatusEnumAlias::{}", output);
  }
};

#endif
