// WARNING: This file is machine generated by fidlgen.

// fidl_experiment = output_index_json

#include <fidl/test.zxtypes/cpp/common_types.h>

#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wshadow"

size_t fidl::internal::DisplayError<::test_zxtypes::StatusEnum>::Format(
    const ::test_zxtypes::StatusEnum& value, char* destination, size_t capacity) {
  const char* member_name = [&]() -> const char* {
    switch (static_cast<int32_t>(value)) {
      case 0u:
        return "OK";
      default:
        return "[UNKNOWN]";
    }
  }();
  size_t written = snprintf(destination, capacity, "%s.%s (value: %u)",
                            "test.zxtypes/StatusEnum", member_name, static_cast<int32_t>(value));
  return std::min(written, capacity - 1);
}

size_t fidl::internal::DisplayError<::test_zxtypes::StatusEnumAlias>::Format(
    const ::test_zxtypes::StatusEnumAlias& value, char* destination, size_t capacity) {
  const char* member_name = [&]() -> const char* {
    switch (static_cast<int32_t>(value)) {
      case 0u:
        return "OK";
      default:
        return "[UNKNOWN]";
    }
  }();
  size_t written = snprintf(destination, capacity, "%s.%s (value: %u)",
                            "test.zxtypes/StatusEnumAlias", member_name, static_cast<int32_t>(value));
  return std::min(written, capacity - 1);
}

#pragma clang diagnostic pop
